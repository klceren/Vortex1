# ── Core web ──────────────────────────────────────────────
flask==3.1.0
gunicorn==23.0.0
Werkzeug==3.1.3

# ── Video indirme ──────────────────────────────────────────
yt-dlp==2026.3.17

# ── QR kod ────────────────────────────────────────────────
qrcode==8.2
pillow==11.3.0

# ── Dosya / medya yardımcıları ────────────────────────────
requests==2.32.5
python-multipart==0.0.22

# ── SSL sertifika üretimi (yerel mod için, cloud'da kullanılmaz) ──
cryptography==46.0.0

# ── Clipboard (Linux'ta xclip gerekir, sadece local) ──────
pyperclip==1.9.0

# ── OpenCV — kamera kaydı için (headless = GUI yok) ───────
opencv-python-headless==4.11.0.86
numpy==1.26.4

# ── Ses/video dönüştürme (ffmpeg binary Render'da mevcut) ─
# ffmpeg sisteme kurulu gelir, pip paketi gerekmez

# ── Diğer yardımcılar ─────────────────────────────────────
python-dateutil==2.9.0.post0
pytz==2026.1

firebase-admin
